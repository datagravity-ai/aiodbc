name: Rust core

# Fast checks for the Rust implementation of aiodbc: lint, build, import smoke
# test, and the SQLite suite (the only backend that needs no server).  The full
# database matrix (SQL Server, PostgreSQL, MySQL, across Python versions) runs in
# the "Ubuntu build" workflow against the same maturin-built wheel.

# include "workflow_dispatch" so this workflow can be run manually from the Actions portal
on: [push, pull_request, workflow_dispatch]

jobs:
  build_and_test:
    name: Build and smoke-test the Rust core
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4.3.0

    - name: Set up Python
      uses: actions/setup-python@v5.0.0
      with:
        python-version: "3.12"

    - name: Set up Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy

    - name: Install unixODBC and the SQLite ODBC driver
      run: |
        sudo apt-get update
        sudo apt-get install -y unixodbc-dev libsqliteodbc

    - name: Lint
      run: |
        cargo fmt --check
        cargo clippy --all-targets -- -D warnings

    - name: Build the wheel
      run: |
        python -m pip install --upgrade pip maturin
        maturin build --release --out dist
        python -m pip install dist/*.whl

    - name: Smoke test
      run: |
        cd /tmp   # keep the repo's python/ source tree off the import path
        python - <<'EOF'
        import aiodbc

        print('version:', aiodbc.version)
        assert aiodbc.apilevel == '2.0'
        assert aiodbc.paramstyle == 'qmark'

        # constants come from the generated table
        assert aiodbc.SQL_VARCHAR == 12
        assert aiodbc.SQL_WMETADATA == -888
        assert aiodbc.SQLWCHAR_SIZE == 2

        # DB API exception hierarchy
        assert issubclass(aiodbc.OperationalError, aiodbc.DatabaseError)
        assert issubclass(aiodbc.DatabaseError, aiodbc.Error)
        assert not issubclass(aiodbc.Warning, aiodbc.Error)

        drivers = aiodbc.drivers()
        print('drivers:', drivers)
        assert isinstance(drivers, list)
        assert any('SQLite' in d for d in drivers)

        sources = aiodbc.dataSources()
        print('dataSources:', sources)
        assert isinstance(sources, dict)
        try:
            aiodbc.dataSources(scope='bogus')
        except ValueError:
            pass
        else:
            raise AssertionError('expected ValueError for bad scope')

        print('smoke test passed')
        EOF

    - name: Async SQLite round-trip
      run: |
        cd /tmp
        python - <<'EOF'
        import asyncio, tempfile
        import aiodbc

        async def main():
            tmpdir = tempfile.mkdtemp()
            async with aiodbc.connect(f'driver=SQLite3;database={tmpdir}/ci.db') as cnxn:
                cursor = cnxn.cursor()
                await cursor.execute('create table t1(n int, s varchar(20))')
                await cursor.executemany(
                    'insert into t1 values (?, ?)', [(i, f'row{i}') for i in range(5)])
                rows = await (await cursor.execute('select n, s from t1 order by n')).fetchall()
                assert [r.n for r in rows] == list(range(5))
                assert rows[3].s == 'row3'
                assert cursor.description[0][0] == 'n'
                total = 0
                async for row in await cursor.execute('select n from t1'):
                    total += row[0]
                assert total == 10
                try:
                    await cursor.execute('select * from nonexistent_table')
                except aiodbc.Error as e:
                    assert isinstance(e.args[0], str)
                else:
                    raise AssertionError('expected aiodbc.Error')
            assert not cnxn.closed  # __aexit__ commits, does not close
            await cnxn.close()
            print('async round-trip passed')

        asyncio.run(main())
        EOF

    - name: Run the SQLite test suite
      run: |
        pip install -r requirements-dev.txt
        python -m pytest tests/sqlite_test.py --no-header -q

    - name: Check generated constants are up to date
      run: |
        python utils/generate-odbc-constants.py
        git diff --exit-code rust/constants.rs
